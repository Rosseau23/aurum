# Backend (Node/Express) environment example.
# Copy to `.env` and adjust as needed.

PORT=4000
JWT_SECRET=replace-with-a-strong-random-secret
CORS_ORIGINS=*

# If you wire up a real upstream provider for diamonds/gift cards,
# add its credentials here and update `routes/*.js` accordingly.
# PROVIDER_API_KEY=
# PROVIDER_API_URL=
